@model WebAOMS.Models.RegisterByPgasViewModel
@{
    ViewBag.Title = "Register";
    Layout = "~/Views/Shared/_Layout_default.cshtml";
}

<div class="box"  style="max-width:400px">
    <div class="box-head" style="border-bottom:1px solid #e8e8e8">
        <header>
            <h4 class="text-bold">@ViewBag.Title.</h4>
        </header>
    </div>
    <div class="box-body">
        <div class="ant-card-body">
            @using (Html.BeginForm("RegisterHRISUser", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form", style = "padding-top:5px" }))
            {
                @Html.AntiForgeryToken()
                @*<h5></h5>*@
                <div class="well">
                    <span class="label label-success"><i class="fa fa-info"></i></span>
                    <span>
                        Create a new account using your HRIS Account.
                    </span>
                </div>
                @Html.ValidationSummary("", new { @class = "text-danger" })

                <div class="PvCIH">
                    <span data-lokalise="true" class="khIjp">
                        @Html.LabelFor(m => m.Email)
                    </span>
                </div>
                <div class="byexym liZmnb" style="padding-bottom:5px">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                </div>


                @*<div class="form-group">
                    
                    <div class="col-md-10">
                        
                    </div>
                </div>*@

                <div class="form-group">
                    <div class="col-md-10">
                        <input type="submit" value="Register" class="btn btn-default" />
                    </div>
                </div>
            }
        </div>
    </div>

</div>


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
